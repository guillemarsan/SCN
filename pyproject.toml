
[project]
name = "SCN"
version = "0.0.1"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[tool.isort]
profile="black"

[project.optional-dependencies]
docs = [
    "sphinx",
    "pydata_sphinx_theme",
]
